package com.zxin.basemodel.gen;

import android.arch.persistence.room.Dao;
import android.arch.persistence.room.Delete;
import android.arch.persistence.room.Insert;
import android.arch.persistence.room.Query;
import android.arch.persistence.room.Update;

import com.zxin.basemodel.entity.HttpUrl;

import java.util.List;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.

/**
 * DAO for table "HTTP_URL".
 */
@Dao
public interface HttpUrlDao {
    @Query("SELECT * FROM tb_httpurl ORDER BY create_timer desc")
    List<HttpUrl> getAll();

    @Query("SELECT * FROM tb_httpurl where is_effective = 1 and order_num != -1 ORDER BY order_num desc")
    List<HttpUrl> getTitleMainList();

    @Query("SELECT * FROM tb_httpurl where is_effective = 1 and order_num != -1 ORDER BY order_num desc LIMIT 1")
    HttpUrl getTitleMain();

    @Query("SELECT * FROM tb_httpurl where id = :urlId LIMIT 1")
    HttpUrl getHttpUrlById(long urlId);

    @Query("SELECT * FROM tb_httpurl where name = :mName LIMIT 1")
    HttpUrl getHttpUrlByName(String mName);

    @Query("SELECT * FROM tb_httpurl LIMIT :current,:count ")
    List<HttpUrl> getByLimit(int current, int count);

    @Query("SELECT COUNT(*) FROM tb_httpurl")
    int getCount();

    @Insert
    long insert(HttpUrl... entities);

    @Delete
    void delete(HttpUrl entity);

    @Query("DELETE FROM tb_httpurl")
    void deleteAll();

    //返回int数据表示删除了多少条。非主键uid值。
    @Query("DELETE FROM tb_httpurl where id = :urlId")
    int deleteById(long urlId);

    //表示更新了多少条目
    @Update
    int update(HttpUrl entity);

    @Query("UPDATE tb_httpurl SET times = :mTimes and last_time = :lastTime where id = :mId")
    int update(long mId, long mTimes,String lastTime);

}
